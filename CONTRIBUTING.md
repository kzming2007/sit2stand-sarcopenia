# 협업 기준

이 저장소는 경진대회 코드, 검증 결과와 제출 문서를 함께 관리한다. 원본 데이터와 촬영 영상은 저장소 밖에서 관리한다.

## Branch와 검토

1. 작업 전에 `main`을 최신 상태로 맞춘다.
2. 한 작업은 한 branch에서 진행한다.
3. 분석 조건이나 결론이 달라지는 변경은 pull request에서 상대 팀원의 확인을 받는다.
4. `main`에는 재실행 방법과 해석 범위가 기록된 작업만 병합한다.
5. `main` 강제 push와 이미 공유한 결과 파일의 덮어쓰기는 하지 않는다.

권장 branch 이름:

- `analysis/<주제>`: 공개자료·통계 분석
- `experiment/<주제>`: Kinect·스마트폰 실험
- `docs/<주제>`: 계획서·보고서·발표 문서
- `fix/<주제>`: 기존 코드나 결과의 오류 수정

## 폴더 책임

| 경로 | 관리 대상 |
|---|---|
| `docs/` | 연구 설계, 실험 기록, 결과 해석, 보고서 초안 |
| `scripts/` | 다시 실행할 수 있는 분석·검증 코드 |
| `results/` | 검토가 끝난 소형 표·JSON·그림 |
| 저장소 밖 | 공개 데이터 원본, 직접 촬영 영상, SDK·대형 모델, 개인정보 자료 |

기존 파일은 현재 위치를 유지한다. 새 결과가 늘어나면 `results/<gate-or-analysis>/` 하위 폴더를 사용하되, 기존 결과를 정리 목적으로 이동하지 않는다.

## Commit 기준

- 한 commit에는 한 가지 목적만 담는다.
- 메시지는 `분석`, `실험`, `문서`, `수정` 중 성격이 드러나게 작성한다.
- 분석 결과 commit에는 데이터 범위, 실행 명령, 평가 단위와 해석 제한을 함께 남긴다.
- 자동 생성된 대형 중간 파일은 commit하지 않는다.
- 얼굴·신체가 포함된 원본 영상과 개인정보 문서는 commit하지 않는다.

예시:

```text
분석: 반복 시행별 시간축 특징 요약 추가
실험: Kinect 동시 촬영 1차 결과 기록
문서: 최종 보고서 평가방법 보정
수정: 교차검증 신뢰구간 계산 방식 변경
```

## 분석 명칭

현재 공개자료 기준 분석의 코드상 `M0~M3` 이름은 호환성을 위해 유지한다. 문서에서 다른 비교실험과 함께 설명할 때는 다음 별칭을 사용한다.

- `B0~B3`: 현재 저장소의 참여자별 요약 특징 기준 분석
- `R0~R2`: 시행별 반복·시간축 특징 후속 분석

`R0~R2`는 아직 최종 결과가 아니다. 실행 계약과 팀 합의 후 별도 branch에서 구현한다.
